<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.powerfuljava.mapper.TravellMapper">
	<select id="getList" resultType="com.powerfuljava.domain.TravellVO">
		<![CDATA[select * from travell_tbl where t_bno >0]]>
	</select>
	<sql id="galleryPaging">
		<where>
		 	<trim prefix="(" suffix=")" prefixOverrides="OR">
			<!-- <trim prefix="(" suffix=")" prefixOverrides="AND | OR">-->
				<foreach item="type" collection="typeArr">
					<trim prefix="OR">
					<!-- <trim prefix="AND">-->
						<choose>
							<when test="type == 'T'.toString()">
								title like CONCAT('%', #{keyword}, '%')
							</when>
							<when test="type == 'C'.toString()">
								content like CONCAT('%', #{keyword}, '%')
							</when>
							<when test="type == 'W'.toString()">
								writer like CONCAT('%', #{keyword}, '%')
							</when>
						</choose>
					</trim>
				</foreach>
			</trim>
		</where>
	</sql>
	<select id="getListWithPaging" resultType="com.powerfuljava.domain.TravellVO">
		select @rownum := @rownum+1 as rn, t_bno, title, content, writer, area, address, kind, regdate, updatedate, replycnt from jdongsu_db.travell_tbl, (select @rownum:=0) A
		<include refid="galleryPaging"></include>
		 order by rn desc limit #{offset}, #{amount}
	</select>
			
	<insert id="insert" useGeneratedKeys="true" keyProperty="t_bno">
		insert into travell_tbl (title, content, writer, area, address, kind)
		values (#{title}, #{content}, #{writer}, #{area}, #{address}, #{kind})
	</insert>
	
	<update id="update">
		update travell_tbl
		set 
		title = #{title}, 
		content = #{content}, 
		writer = #{writer},
		area = #{area},
		address = #{address},
		kind = #{kind}, 
		updatedate = now()
		where t_bno = #{t_bno}
	</update>
	
	<delete id="delete">
		delete from travell_tbl where t_bno = #{t_bno}
	</delete>
	
	<select id="read" resultType="com.powerfuljava.domain.TravellVO">
		select * from travell_tbl where t_bno = #{t_bno}
	</select>
	
	<select id="getTotalCount" resultType="int">
		select count(*) from travell_tbl 
		where
		 <trim suffix="and" prefixOverrides="WHERE">
			<include refid="galleryPaging"></include>
		</trim> 
		t_bno > 0
	</select>
	
	<update id="updateReplyCnt">
		update travell_tbl set replycnt = replycnt + #{amount} where t_bno = #{t_bno}
	</update>
	
	<!-- 메인 페이지에 최신글 출력 -->
	<select id="getLatestTravellList" resultType="com.powerfuljava.domain.TravellVO">
		 select @rownum := @rownum+1 as rn, t_bno, title, writer, regdate, area, kind
		from jdongsu_db.travell_tbl, (select @rownum:=0) A
		<include refid="galleryPaging"></include>
		 order by regdate desc limit 6 
	</select>
</mapper>